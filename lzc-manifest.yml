application:
  subdomain: cfui
  background_task: true
  public_path:
    - /

  upstreams:
    - location: /
      backend: http://host.lzcapp:14333/

services:
  cfui:
    network_mode: host
    user: root
    # czyt/cfui:v0.8.22
    image: registry.lazycat.cloud/czyt/czyt/cfui:c3d5cb8f16640586
    binds:
      - /lzcapp/var/data:/app/data
      - /lzcapp/var/logs:/app/logs
    environment:
      - BIND_HOST=
      - PORT=14333
      - DATA_DIR=/app/data
